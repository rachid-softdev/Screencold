# Screencold — E2E Test Scenarios: Wave 4 — Security & Edge Cases

> Generated by test-automation-engineer analyzing middleware, CSRF, rate limiting, session management, IDOR patterns, and error boundaries across the full codebase.
> Total: **43 scenarios** across 7 security/edge domains.

---

## Contents

1. [CSRF Protection](#1-csrf-protection)
2. [Session Expiry & Token Management](#2-session-expiry--token-management)
3. [Rate Limiting](#3-rate-limiting)
4. [Input Sanitization & XSS](#4-input-sanitization--xss)
5. [IDOR (Insecure Direct Object Reference)](#5-idor-insecure-direct-object-reference)
6. [Concurrent Operations](#6-concurrent-operations)
7. [Error Boundary (deep)](#7-error-boundary-deep)

---

## 1. CSRF Protection

| Prio | Cat | Test Name | Description |
|------|-----|-----------|-------------|
| P0 | CSRF | CSRF_missing_origin_on_POST_to_audits | POST /api/audits without Origin header, with cross-origin Referer → 403 `CSRF_FAILED` or `CSRF_TOKEN_INVALID` |
| P0 | CSRF | CSRF_cross_origin_Referer_on_campaign_POST | POST /api/campaigns with `Referer: https://evil.com` → 403 `CSRF_FAILED` |
| P0 | CSRF | CSRF_cross_origin_Origin_on_email_template_POST | POST /api/email-templates with `Origin: https://evil.com` → 403 `CSRF_FAILED` |
| P0 | CSRF | CSRF_valid_same_origin_POST_is_allowed | POST /api/campaigns with same-origin Referer (from APP_URL) → 201, campaign created successfully |
| P1 | CSRF | CSRF_DELETE_without_origin_is_blocked | DELETE /api/campaigns/:id without Origin header and cross-origin Referer → 403 |
| P1 | CSRF | CSRF_PATCH_without_origin_on_audit | PATCH /api/audits/:id with `Origin: https://attacker.com` → 403 `CSRF_FAILED` |
| P2 | CSRF | CSRF_login_page_has_csrf_form_field | Navigate to /login → page includes CSRF token hidden input field |

---

## 2. Session Expiry & Token Management

| Prio | Cat | Test Name | Description |
|------|-----|-----------|-------------|
| P0 | Session | Session_expired_redirects_dashboard_to_login | Clear cookies (session token), navigate to /dashboard → redirect to /login |
| P0 | Session | Session_expired_api_returns_401 | Clear cookies, GET /api/dashboard → 401 `UNAUTHORIZED` |
| P0 | Session | Session_expired_audits_api_returns_401 | Clear cookies, POST /api/audits → 401 `UNAUTHORIZED` |
| P0 | Session | Session_valid_allows_protected_access | Authenticated user can GET /api/dashboard → 200 with user stats |
| P1 | Session | Session_removing_nextauth_session_cookie_redirects_login | Remove `next-auth.session-token` cookie, navigate to /campaigns → redirect to /login |
| P1 | Session | Session_api_auth_session_returns_null_when_unauthenticated | Clear cookies, GET /api/auth/session → 200 with `{user: null}` |
| P1 | Session | Session_persists_across_page_navigations | Authenticated → navigate to /dashboard, /campaigns, /audits/new → all load without redirect |
| P2 | Session | Session_expired_token_on_protected_api_route | Tamper `next-auth.session-token` cookie with invalid value, POST /api/campaigns → 401 or redirect |

---

## 3. Rate Limiting

| Prio | Cat | Test Name | Description |
|------|-----|-----------|-------------|
| P0 | RateLimit | RateLimit_login_rapid_sequential | Send 15+ rapid POST /api/auth/register requests → at least one returns 429 `RATE_LIMITED` |
| P0 | RateLimit | RateLimit_audit_creation_rapid | Send 35+ rapid POST /api/audits requests (authenticated) → at least one returns 429 |
| P1 | RateLimit | RateLimit_contact_form_spam | Send 10+ rapid POST /api/contact requests → at least one returns 429 |
| P1 | RateLimit | RateLimit_rate_limit_headers_present_on_429 | When rate limited, response includes `X-RateLimit-Remaining: 0` and `Retry-After` or `X-RateLimit-Reset` |
| P1 | RateLimit | RateLimit_forgot_password_rapid | Send 15+ rapid POST /api/auth/forgot-password requests → at least one returns 429 |
| P2 | RateLimit | RateLimit_normal_requests_succeed_below_threshold | Send 5 sequential POST /api/audits requests → all return 201 or 400 (validation error), no 429 |

---

## 4. Input Sanitization & XSS

| Prio | Cat | Test Name | Description |
|------|-----|-----------|-------------|
| P0 | XSS | XSS_script_injection_in_audit_url | POST /api/audits with `url: "https://example.com/<script>alert(1)</script>"` → 201 (URL validated, injection stored safely, not executed) |
| P0 | XSS | XSS_script_in_campaign_name | POST /api/campaigns with `name: "<script>alert('xss')</script>"` → 201, campaign created with name stored safely |
| P1 | XSS | XSS_html_in_prospect_contactName | POST /api/campaigns/:id/prospects with `contactName: "<b>Bold Name</b>"` → 200, stored safely |
| P1 | XSS | XSS_html_in_prospect_contactEmail | POST /api/campaigns/:id/prospects with `contactEmail: "<img src=x onerror=alert(1)>@x.com"` → 400 VALIDATION_ERROR (invalid email) |
| P1 | XSS | XSS_special_chars_in_campaign_name | POST /api/campaigns with `name: "Campagne spéciale été 2024! @#$%^&*()"` → 201, created successfully |
| P1 | XSS | XSS_html_in_contact_form_name | POST /api/contact with `name: "<script>document.cookie</script>"` → 201, stored safely |
| P1 | XSS | XSS_html_in_audit_url_field_stored_safely | POST /api/audits with url containing HTML (e.g., `https://example.com\"/><script>`) → 400 or 201 but stored encoded |
| P2 | XSS | XSS_unicode_and_emoji_in_campaign_name | POST /api/campaigns with `name: "你好世界! 😊 Campaign"` → 201, stored correctly |

---

## 5. IDOR (Insecure Direct Object Reference)

| Prio | Cat | Test Name | Description |
|------|-----|-----------|-------------|
| P0 | IDOR | IDOR_another_users_audit_by_id_returns_403 | User A creates audit, User B GET /api/audits/:userA-auditId → 403 `FORBIDDEN` |
| P0 | IDOR | IDOR_another_users_campaign_by_id_returns_403 | User A creates campaign, User B GET /api/campaigns/:userA-campaignId → 403 `FORBIDDEN` |
| P0 | IDOR | IDOR_another_users_audit_PATCH_returns_403 | User A creates audit, User B PATCH /api/audits/:userA-auditId → 403 `FORBIDDEN` |
| P0 | IDOR | IDOR_another_users_campaign_DELETE_returns_403 | User A creates campaign, User B DELETE /api/campaigns/:userA-campaignId → 403 `FORBIDDEN` |
| P1 | IDOR | IDOR_another_users_email_template_returns_404_or_403 | User A creates email template, User B PATCH /api/email-templates/:userA-templateId → 403 `FORBIDDEN` or 404 |
| P1 | IDOR | IDOR_another_users_billing_transactions_not_accessible | User A has billing transactions, User B cannot see User A's transactions (api/billing/transactions filtered by userId) |
| P1 | IDOR | IDOR_own_audit_and_campaign_accessible | User A can GET own audit details → 200, can GET own campaign → 200 |
| P2 | IDOR | IDOR_nonexistent_audit_returns_404 | Authenticated user GET /api/audits/fake-nonexistent-id → 404 `NOT_FOUND` |
| P2 | IDOR | IDOR_nonexistent_campaign_returns_404 | Authenticated user GET /api/campaigns/fake-nonexistent-id → 404 `NOT_FOUND` |

---

## 6. Concurrent Operations

| Prio | Cat | Test Name | Description |
|------|-----|-----------|-------------|
| P0 | Concurrent | Concurrent_double_click_create_audit_only_creates_one | Click "Create Audit" twice rapidly → only one audit created, no duplicate record |
| P1 | Concurrent | Concurrent_accept_same_invitation_twice_returns_error | Accept team invitation, then accept again → second accepts returns 400 `ALREADY_MEMBER` |
| P1 | Concurrent | Concurrent_two_campaigns_same_name_both_created | Create campaign "Duplicate Name", create another with same name → both created successfully (no uniqueness constraint) |
| P2 | Concurrent | Concurrent_rapid_audit_creation_same_url_multiple | POST same URL to /api/audits 3 times in parallel → all 3 return 201 with different auditIds (no dedup) |

---

## 7. Error Boundary (deep)

| Prio | Cat | Test Name | Description |
|------|-----|-----------|-------------|
| P0 | Error | Error_navigate_nonexistent_audit_detail_shows_404 | Navigate to /audits/fake-nonexistent-id → 404 page shown |
| P0 | Error | Error_navigate_invalid_route_shows_404 | Navigate to /this-route-does-not-exist-at-all → 404 page |
| P0 | Error | Error_admin_page_non_admin_redirects_or_403 | Non-admin user navigates to /admin/observability → redirect to dashboard or 403 |
| P1 | Error | Error_api_nonexistent_route_returns_404 | GET /api/nonexistent-route → 404 |
| P1 | Error | Error_navigate_to_settings_billing_shows_page | Authenticated user navigates to /settings/billing → page loads without error |
| P1 | Error | Error_api_nonexistent_audit_detail_returns_404 | GET /api/audits/nonexistent-id → 404 `NOT_FOUND` |
| P2 | Error | Error_authenticated_campaign_nonexistent_404 | Navigate to /campaigns/nonexistent-campaign-id → 404 page shown |

---

## Summary

| Domain | P0 | P1 | P2 | Total |
|--------|----|----|----|-------|
| CSRF Protection | 4 | 2 | 1 | 7 |
| Session Expiry & Token Management | 4 | 3 | 1 | 8 |
| Rate Limiting | 2 | 3 | 1 | 6 |
| Input Sanitization & XSS | 2 | 5 | 1 | 8 |
| IDOR | 5 | 3 | 2 | 10 |
| Concurrent Operations | 1 | 2 | 1 | 4 |
| Error Boundary | 3 | 3 | 1 | 7 |
| **Total** | **21** | **21** | **8** | **50** |
